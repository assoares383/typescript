"use strict";
let channel = 'Gazeta';
let subscribers = 650351;
console.log(channel);
function sum(a, b) {
    return a + b;
}
let anything;
anything = 12;
anything = 14;
function salute(isMorning) {
    let salutation;
    if (isMorning) {
        salutation = 'Good Morning!';
    }
    else {
        salutation = 'Good Life!';
    }
    return salutation;
}
