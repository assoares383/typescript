let channel: string = 'Gazeta';
let subscribers: number = 650351;

console.log(channel);

function sum(a: any,b: any) {
  return a + b;
}

let anything;
anything = 12;
anything = 14;

function salute(isMorning:boolean): string {
  let salutation: string;
  if(isMorning) {
    salutation = 'Good Morning!';
  } else {
    salutation = 'Good Life!'
  }

  return salutation;
}
